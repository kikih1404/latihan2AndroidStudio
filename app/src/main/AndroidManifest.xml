<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.example.projectnewsactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Latihan2_dafi">
        <activity android:name=".ui.halaman1"></activity>
        <activity android:name=".ui.video" />
        <activity android:name=".ui.daftar" />
        <activity android:name=".ui.MenuActivity" />
        <activity android:name=".ui.splesh" />
        <activity android:name=".ui.newsactivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>